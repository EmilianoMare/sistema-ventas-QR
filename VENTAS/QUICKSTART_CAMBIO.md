# Quickstart Tipo de Cambio (Deshabilitado)

El sistema ahora opera únicamente en ARS. El quickstart para el módulo de tipo de cambio fue retirado.

Para restaurar: activa el módulo en `config/app.php` y restaura los archivos `app/controllers/cambioController.php` y `app/views/content/cambio-view.php`.
